"use client";

import { SearchPageContent } from "@/components/SearchPageContent";

/**
 * Home page component
 * Main page for GitHub users search functionality (no search query)
 */
export default function Home() {
  return <SearchPageContent />;
}
